// object literal

/*const cliente = {
    nombre: "Jesus Lopez",
    saldo: 1711,
    tipoCliente: function(){
        let tipo;
        if(this.saldo > 1000){
            tipo = 'Gold';
        } else if(this.saldo > 500) {
            tipo = 'Platinimu';
        } else {
            tipo = 'Normal';
        }

        return tipo;
    }
}

console.log(cliente.tipoCliente());*/

// Metodo alternatico 